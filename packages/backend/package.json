{
  "name": "@workspace/backend",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "convex dev ",
    "check": "eslint --cache --cache-location .eslintcache --concurrency auto . && tsc --noEmit --project tsconfig.json",
    "fix": "eslint --cache --cache-location .eslintcache --concurrency auto --fix . && tsc --noEmit --project tsconfig.json",
    "clean": "rm -rf node_modules .turbo .eslintcache"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "@convex-dev/resend": "^0.1.13",
    "@workspace/utils": "workspace:*",
    "better-auth": "catalog:",
    "convex": "catalog:"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "@workspace/eslint-rules": "workspace:*",
    "eslint": "catalog:",
    "eslint-import-resolver-typescript": "catalog:",
    "eslint-plugin-boundaries": "catalog:",
    "eslint-plugin-import": "catalog:",
    "typescript": "catalog:"
  }
}
