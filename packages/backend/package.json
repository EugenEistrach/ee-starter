{
  "name": "@workspace/backend",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "convex dev ",
    "check": "eslint --cache --cache-location .eslintcache --concurrency auto convex/ && tsc --noEmit --project convex/tsconfig.json",
    "fix": "eslint --cache --cache-location .eslintcache --concurrency auto --fix convex/ && tsc --noEmit --project convex/tsconfig.json",
    "clean": "rm -rf node_modules .turbo .eslintcache"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "better-auth": "catalog:",
    "convex": "catalog:"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
