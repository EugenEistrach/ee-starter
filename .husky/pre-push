echo "🔧 Auto-fixing issues before push..."
bun run fix || true

echo "🔍 Running quality checks..."
if ! bun run check; then
  echo ""
  echo "⚠️  Code quality checks failed. Cannot push."
  echo "You must:"
  echo "- Remove unused imports/variables/code"
  echo "- Make unused exports private or remove them"
  echo "- Fix all linting errors properly"
  echo "- DO NOT use --no-verify to bypass these checks"
  echo ""
  echo "If stuck on an issue, commit your WIP and ask for help."
  echo "Re-run checks until clean before pushing."
  echo ""
  exit 1
fi

echo "✅ All checks passed. Pushing..."
